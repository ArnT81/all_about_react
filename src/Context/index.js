import React from 'react';

/**
*   This is not an "default" export since you need to declare the variable, which would require another row of code. You
*   need curly brackets ({}) when you import it.
*   Example:
*            const Context = React.createContext({});
*            export default Context;
**/

export const Context = React.createContext({});